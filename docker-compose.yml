services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: depth
      POSTGRES_PASSWORD: depth
      POSTGRES_DB: depth
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: